var path=require("path"),
		types=require("./type");
const TYPE_HTML="html";
const UNKNOWN="unknown";

function route(pathname, request, response){
	//set the default entrance
	if(pathname==="/"||pathname===""){
		pathname="/"
	}
}